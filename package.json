{
  "name": "cookiedle",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "lint": "eslint worker.js worker/**/*.js docs/**/*.js",
    "lint:fix": "eslint --fix worker.js worker/**/*.js docs/**/*.js",
    "format": "prettier --write worker.js worker/**/*.js docs/**/*.js docs/**/*.css docs/**/*.html data/**/*.json",
    "format:check": "prettier --check worker.js worker/**/*.js docs/**/*.js docs/**/*.css docs/**/*.html data/**/*.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "seed-kv": "wrangler kv key put \"cookies\" --binding COOKIEDLE_KV --path data/cookies.json"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.3.0",
    "eslint-config-prettier": "^10.1.8",
    "globals": "^17.6.0",
    "prettier": "^3.8.3",
    "vitest": "^4.1.0",
    "wrangler": "^4.103.0"
  }
}
